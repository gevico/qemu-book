\part{QEMU 从哪里来}

QEMU 今天已经能描述处理器、内存、总线、设备和加速器，也能把一台运行中的虚拟机交给迁移框架。这些抽象并不是项目诞生时画好的一张总图。最早的问题小得多：一段为 x86 Linux 编译的程序，怎样在 PowerPC 或 Arm Linux 上运行？当用户进一步要求启动内核、连接设备、切换执行引擎时，原有边界才一层层向外扩展。本篇沿着这条问题链回到 2003 年，再用 RISC-V `virt` 和 `v11.1.0-rc0` 的源码检查每次扩展留下的工程契约。
