\part{怎样让复杂系统可调试、可验证、可演进}

模型能够启动，只说明一条正常路径暂时走通。长期维护还要在故障现场找到状态所有者，用测试守住 reset、迁移和兼容契约，让补丁在公开审查中暴露假设，并判断新语言能否真正降低维护风险。本篇以 RISC-V 的裸机、RTOS、Linux、K230 与教学设备贯穿这些问题，把 monitor、log、trace event、gdb stub、测试矩阵、邮件协作和 Rust 接成一条从定位到演进的方法链。
