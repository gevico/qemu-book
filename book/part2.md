\part{TCG：把翻译器变成可维护的编译器}

dyngen 已经证明动态翻译可以很快，QEMU 为什么还要在 2008 年把代码生成能力重新收回项目内部？本篇从旧方案受宿主编译器牵制的现场出发，沿 RISC-V 指令的译码、TCG IR、优化、宿主发射、SoftMMU 与 MTTCG 逐层回答。重点不在罗列 op，而在理解一条客户机语义经过多少边界、每个边界为何存在，以及优化为什么必须在精确异常和并发内存模型之前停下。
