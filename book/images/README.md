# 图片目录

`cover.png` 是 PDF 的全页封面底图。作者、版本号和构建日期由
`preamble.tex` 在构建时叠加，其中版本值来自 `BOOK_VERSION`，日期默认
取构建环境的系统时间，也可用 `BOOK_DATE` 覆盖。不要把这些字段直接写进图片。

全书图片统一放在此目录，文件名使用 `chNN-topic.svg` 或 `chNN-topic.png`。

优先使用 SVG。图片中使用的字体应当开源且可嵌入；提交前请在生成的 PDF 中检查文字、线条、分页和可读性。图片源文件如果不是 SVG，请放在以相同名称命名的子目录中，并在章节旁注明生成方式。
